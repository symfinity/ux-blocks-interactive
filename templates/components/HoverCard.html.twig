<div
    data-ui-role="hover-card"
    data-ui-fragment="blocks.int.hover-card"
    data-controller="symfony--ux-blocks-interactive--hover-card"
    data-symfony--ux-blocks-interactive--hover-card-open-delay-value="{{ openDelay }}"
    data-symfony--ux-blocks-interactive--hover-card-close-delay-value="{{ closeDelay }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>
