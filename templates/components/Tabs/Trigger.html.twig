{% if href %}
<a
    role="tab"
    href="{{ href }}"
    data-ui-role="tabs-trigger"
    data-ui-state="linked"
    data-value="{{ value }}"
    aria-selected="{{ active ? 'true' : 'false' }}"
    {% if disabled %}aria-disabled="true" tabindex="-1"{% endif %}
    data-symfinity--ux-blocks-interactive--tabs-target="trigger"
    {{ attributes }}
>
{% else %}
<button
    type="button"
    role="tab"
    data-ui-role="tabs-trigger"
    data-value="{{ value }}"
    aria-selected="{{ active ? 'true' : 'false' }}"
    {% if disabled %}disabled aria-disabled="true" data-ui-state="disabled"{% endif %}
    data-action="click->symfinity--ux-blocks-interactive--tabs#select"
    data-symfinity--ux-blocks-interactive--tabs-target="trigger"
    {{ attributes }}
>
{% endif %}
    {% block content %}{% endblock %}
{% if href %}
</a>
{% else %}
</button>
{% endif %}
