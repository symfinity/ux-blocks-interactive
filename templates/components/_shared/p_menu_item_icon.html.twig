{# Menu item leading icon — mirrors core p_icon_slot semantics. #}
{% if iconName|default(null) %}
    {% include '@UxBlocksCore/components/_shared/p_icon_slot.html.twig' with {
        iconName: iconName,
        iconDecorative: iconDecorative|default(true),
    } only %}
{% endif %}
